import React from "react";
import { HeaderStyle, TitleStyled, LogoutStyled } from "./layout";

const Header = () => {
    return (
        <HeaderStyle>
            <TitleStyled>Easy House</TitleStyled>
        </HeaderStyle>
    );
}

export default Header;